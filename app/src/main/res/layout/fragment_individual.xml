<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background1"
    android:orientation="vertical"
    tools:context=".IndividualFragment">

    <!-- TODO: Update blank fragment layout -->

    <ImageView
        android:id="@+id/background_img"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/swufe"/>

    <TextView
        android:id="@+id/individualText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="100dp"
        android:gravity="center"
        android:text="@string/hello_individual_fragment"
        android:textColor="@color/black"
        android:textSize="20dp" />

    <Button
        android:textColor="@color/light_black"
        android:background="@drawable/button_cs1"
        android:padding="15dp"
        android:id="@+id/background_change"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="更换背景" />

    <ImageView
        android:layout_marginBottom="50dp"
        android:id="@+id/logo"
        android:paddingLeft="15dp"
        android:paddingRight="15dp"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:layout_gravity="center"
        android:src="@drawable/logo3" />




</FrameLayout>